{
  "name": "hardlopen-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Backend van de Hardlopen-add-on: trainingen en loopgeschiedenis bewaren en synchroniseren",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=20"
  }
}
